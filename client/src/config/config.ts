const config = {
    APPNAME: 'MERN Message Board',
    API: 'http://localhost:5000/api',
    JWT_SECRET: 'secret'
}

export const API = config.API
export const APPNAME = config.APPNAME
export const JWT_SECRET = config.JWT_SECRET